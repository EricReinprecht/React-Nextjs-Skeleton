model TicketClass {
    id              String    @id @default(cuid())
    name            String
    description     String

    tickets         Ticket[]  @relation("TicketToPartyClass")

    partyId         String
    party           Party   @relation(fields: [partyId], references: [id])
}