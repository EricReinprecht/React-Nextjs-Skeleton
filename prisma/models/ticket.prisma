model Ticket {
    id String @id @default(cuid())
    categories     TicketCategory[]  @relation("TicketToCategory")

    partyId   String
    party     Party   @relation(fields: [partyId], references: [id])
}